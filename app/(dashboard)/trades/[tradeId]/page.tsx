import{getTradeDetail}from'@/lib/server/trade-detail';import{CloseTradeForm}from'@/components/trades/close-trade-form';
export default async function Page({params}:{params:Promise<{tradeId:string}>}){const{id}= {id:(await params).tradeId};const t:any=await getTradeDetail(id);return <main className="mx-auto max-w-5xl p-6 md:p-10"><p className="text-xs uppercase tracking-[.18em] text-cyan-300">Operación</p><h1 className="mt-2 text-3xl font-semibold">{t.instrument?.display_name} · {t.direction}</h1><div className="my-6 grid gap-3 sm:grid-cols-3"><Card l="Estado" v={t.status}/><Card l="Riesgo" v={`${t.risk_percentage}% · $${t.risk_amount}`}/><Card l="Lote" v={t.actual_lot}/></div>{t.status==='OPEN'?<CloseTradeForm tradeId={t.id}/>:<div className="rounded-2xl border border-slate-800 bg-slate-950 p-5"><p>Resultado: <b>{t.pnl_usd} USD</b> · R {t.r_multiple??'—'}</p></div>}</main>}
function Card({l,v}:{l:string;v:any}){return <div className="rounded-xl border border-slate-800 bg-slate-950 p-4"><p className="text-xs text-slate-500">{l}</p><p className="mt-1 font-semibold">{v}</p></div>}
