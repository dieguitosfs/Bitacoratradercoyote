import{getCalendarMonth}from'@/lib/server/calendar';import{TradingCalendar}from'@/components/calendar/trading-calendar';
export default async function Page({searchParams}:{searchParams:Promise<{month?:string}>}){const q=await searchParams;const d=await getCalendarMonth(q.month);return <main className="mx-auto max-w-7xl p-6 md:p-10"><p className="text-xs uppercase tracking-[.18em] text-cyan-300">Actividad</p><h1 className="mt-2 text-3xl font-semibold">Calendario de Trading</h1><p className="mb-6 mt-2 text-sm text-slate-400">{d.month} · domingos bloqueados y sábado PM marcado como restringido.</p><TradingCalendar {...d}/></main>}
