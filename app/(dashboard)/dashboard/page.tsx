import Link from'next/link';import{getDashboardData}from'@/lib/server/dashboard';
const f=(v:any,d=2)=>v?.toFixed?v.toFixed(d):Number(v??0).toFixed(d);
export default async function Page(){const d=await getDashboardData();const a=d.account;const m=d.metrics;const cards=[['Balance MT5',a?`$${f(d.current)}`:'—'],['Balance inicial mensual',a?`$${f(d.startBalance)}`:'—'],['Resultado del mes',`$${m.net.toFixed(2)}`],['Crecimiento mensual',`${d.growth.toFixed(2)}%`],['Riesgo actual',d.riskPercentage?`${d.riskPercentage}% · $${d.riskAmount?.toFixed(2)}`:'—'],['Operaciones del mes',String(d.monthTrades)],['Win Rate',`${m.winRate.toFixed(1)}%`],['Profit Factor',m.profitFactor?.toFixed(2)??'∞'],['R:R promedio',m.averageR.toFixed(2)],['Drawdown',`$${m.maxDrawdown.toFixed(2)}`],['Racha ganadora',String(m.maxWinStreak)],['Racha perdedora',String(m.maxLossStreak)]];return <main className="mx-auto max-w-7xl p-6 md:p-10"><div className="flex flex-col gap-4 sm:flex-row sm:justify-between"><div><p className="text-xs uppercase tracking-[.18em] text-cyan-300">Price Action Pro</p><h1 className="mt-2 text-3xl font-semibold">Dashboard</h1><p className="mt-2 text-sm text-slate-400">{d.userEmail}</p></div><Link href="/trades/new" className="h-fit rounded-xl bg-cyan-400 px-4 py-3 font-semibold text-slate-950">Nueva operación</Link></div>{a?<div className="mt-6 rounded-2xl border border-slate-800 bg-slate-950/60 p-5"><p className="text-xs text-slate-500">CUENTA ACTIVA</p><p className="mt-1 text-xl font-semibold">{a.name}</p><div className="mt-3 flex flex-wrap gap-4 text-sm"><span>Bitácora: ${f(a.journal_calculated_balance)}</span><span>Diferencia MT5: ${f(Number(a.mt5_registered_balance)-Number(a.journal_calculated_balance))}</span></div></div>:<Link href="/accounts/new" className="mt-6 block rounded-2xl border border-cyan-800 p-5 text-cyan-200">Crea una cuenta para comenzar.</Link>}<div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([l,v])=><div key={l} className="rounded-2xl border border-slate-800 bg-slate-950/70 p-5"><p className="text-sm text-slate-400">{l}</p><p className="mt-2 text-2xl font-semibold">{v}</p></div>)}</div></main>}
