'use client';import{useState}from'react';
export function PlanSettingsForm({maxMonthlyTrades,allowRiskOverride}:{maxMonthlyTrades:number|null;allowRiskOverride:boolean}){const[max,setMax]=useState(maxMonthlyTrades?.toString()??'');const[override,setOverride]=useState(allowRiskOverride);const[msg,setMsg]=useState('');async function save(){const r=await fetch('/api/trading-plan',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({maxMonthlyTrades:max?Number(max):null,allowRiskOverride:override})});setMsg(r.ok?'Configuración guardada.':'No fue posible guardar.')}return <div className="space-y-4 rounded-2xl border border-slate-800 bg-slate-950/70 p-5"><h2 className="font-semibold">Configuración personal</h2><label>Máximo mensual<input className="field" inputMode="numeric" value={max} onChange={e=>setMax(e.target.value)} placeholder="Ej. 40"/></label><label className="flex items-center gap-3"><input type="checkbox" checked={override} onChange={e=>setOverride(e.target.checked)}/> Permitir override de sobreriego <span className="text-amber-300">(no recomendado)</span></label><button onClick={save} className="rounded-xl bg-cyan-400 px-4 py-2 font-semibold text-slate-950">Guardar</button>{msg&&<p className="text-sm text-slate-400">{msg}</p>}</div>}
